Drop colorway renders here as `<collection>-<finish>.webp`, e.g. `flores-sunshine.webp`.
Export each at the same two widths as the hero (900w and 1492w) with alpha intact.
Until a file exists, `<ProductShot>` draws the finish as an SVG illustration.
